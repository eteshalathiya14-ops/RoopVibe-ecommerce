# TODO - Dynamic category navigation (VMart/Myntra style)

- [ ] Refactor routing: add React Router + AppRoutes with:
  - [ ] `/` -> Home
  - [ ] `/category/:gender/:subcategory` -> Category page
  - [ ] `*` -> Not found
- [ ] Update `src/main.jsx` to wrap app with `<BrowserRouter>`.
- [ ] Remove currentPage/selectedCategory state routing from `src/App.jsx` and render only routes.
- [ ] Refactor `src/component/Navbar.jsx`:
  - [ ] Use `navLinks` from `src/Data/Navdata.jsx` (no duplication)
  - [ ] Convert mega-menu items into `<Link>`/`useNavigate` to `/category/:gender/:subcategory`
  - [ ] Compute active menu from route params and highlight selected menu.
- [ ] Add slug utility `src/utils/slug.js`:
  - [ ] `toSlug(label)` ("Kurta Kurtis" -> "kurta-kurtis")
- [ ] Build category index from `navLinks`:
  - [ ] `src/utils/categoryIndex.js` generates lookup by `{gender, subcategorySlug}`
- [ ] Refactor `src/pages/Categorypage.jsx`:
  - [ ] Read `gender` and `subcategory` from `useParams()`
  - [ ] Derive category title + breadcrumb using category index
  - [ ] Implement fallback UI for unknown category
  - [ ] Replace hardcoded category prop logic with URL-driven logic
- [ ] Implement dynamic product matching:
  - [ ] Create `src/utils/productMatcher.js`
  - [ ] Filter `mockProducts` based on selected `{gender, subcategoryLabel/slug}` without hardcoded per-category conditions
- [ ] Ensure existing filter + sorting UX remains intact.
- [ ] Quick manual QA:
  - [ ] Click mega-menu items -> correct URL
  - [ ] Breadcrumb/title update
  - [ ] Filters & sorting work
  - [ ] Non-existing route shows fallback

